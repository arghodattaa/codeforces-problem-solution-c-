#include<iostream>
using namespace std;
#include<bits/stdc++.h>
int main(){
   int te;
   cin>>te;
   while(te--){
   long long int l,r;
   cin>>l>>r;
   if(l*2<=r)cout<<l<<" "<<2*l<<endl;
   else cout<<-1<<" "<<-1<<endl;
   }
}
